node_modules/
dist/
.env
.env.*
!.env.example
data/*.json
data/*.jsonl
.DS_Store
*.log
.turbo/
coverage/

# Operator vault — private conversation custody (never commit plaintext)
.planning/private/

public/assets/voice/*.mp3

# CopilotKit CLI local project bind (may hold keys)
.copilotkit/
