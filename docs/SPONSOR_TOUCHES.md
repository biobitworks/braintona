# Sponsor touches — each one special (CodeRabbit featured)

**Team:** Second Machine · **Repo:** https://github.com/biobitworks/braintona  
**Rule:** No logo wall. Every sponsor gets a **named hop**, a **demo beat**, and a **custody job**.  
**Featured Best Use lane:** **CodeRabbit** (review / Discord Path B + live PR bot).

---

## Featured — CodeRabbit

| | |
|---|---|
| **Job** | Independent **review observer** on the public custody repo — second pair of eyes that does not rewrite the content leaf |
| **Touch** | `coderabbitai[bot]` on GitHub PRs + Discord Path B (`docs/CODERABBIT_DISCORD_PATH_B.md`) |
| **Custody** | Seal observe hop → `POST /api/coderabbit/seal-observe` → token-trace + graph pointer |
| **Demo line** | “CodeRabbit reviews the public graph of our work — sponsor_system signature, observe-only, fail-soft.” |
| **Devpost** | Public repo + CodeRabbit GitHub bot connected; Discord Agent Path B for Best Use $1k lane |
| **Ceiling** | Observer ≠ PI seal; review comments are not science truth |

**Operator (now):**

```bash
# Request full review (PRs were draft → skipped)
gh pr ready 3 --repo biobitworks/braintona
gh pr comment 3 --repo biobitworks/braintona --body "@coderabbitai review"
# Discord Path B when venue Wi‑Fi OK — docs/CODERABBIT_DISCORD_PATH_B.md
```

---

## Daytona

| | |
|---|---|
| **Job** | **Second machine** — independent sandbox recompute of custody root |
| **Touch** | Ephemeral Python `codeRun` on receipt JSON |
| **Demo** | Green match → planted tamper **fails closed** |
| **Line** | “Same-box verify is the gap. Daytona is the second machine.” |

## Braintrust

| | |
|---|---|
| **Job** | **Eval spine** — gold claim task scores / experiment spans |
| **Touch** | `braintrust` logger → project `braintona-hacksprint` |
| **Demo** | Precision/recall/F1 beside custody (quality ≠ integrity) |
| **Line** | “Braintrust scores the task; custody scores the bytes.” |

## Fireworks AI

| | |
|---|---|
| **Job** | **Inference** — claim extraction (`glm-5p1`, temp 0) |
| **Touch** | Chat completions → content leaf |
| **Demo** | Live pipeline step 1; FireConnect Cursor **off** (promo conserve) |
| **Line** | “Fireworks answers; we seal what it said.” |

## ElevenLabs

| | |
|---|---|
| **Job** | **Voice origin** — AI leaves + two-avatar call |
| **Touch** | Sarah/Matilda TTS → `content_class=ai` trees → interaction MMR |
| **Demo** | Two-avatar + voice-origin contrast; vault tip for customer |
| **Line** | “Component: MMR of actual voice vs AI — labels, not deepfakes.” |

## CopilotKit

| | |
|---|---|
| **Job** | **Operator cockpit** — chat turns as FCO leaves |
| **Touch** | Org → license → `/api/copilotkit/seal-turn` UI panel |
| **Demo** | Seal operator + AI turns → interaction MMR |
| **Line** | “CopilotKit is where the human drives custody.” |
| **Gate** | **LIVE** — license in `.env` (OPERATOR authorized); seal turns on `/` |

## WorkOS

| | |
|---|---|
| **Job** | **Optional human identity** — AuthKit pointer |
| **Touch** | `/login` chip; not vault SoT; not demo gate |
| **Demo** | Optional sign-in; custody works without it |
| **Line** | “Identity pointer — separate from Merkle tip.” |

---

## Live demo order (sponsor-special)

1. **Fireworks** extract  
2. **Braintrust** score  
3. FCO seal → **Daytona** ✓ / tamper ✗  
4. Token trace — call each hop by name  
5. **ElevenLabs** two-avatar / voice MMR  
6. **CodeRabbit** — open PR review / sealed observe hop (**featured**)  
7. **CopilotKit** cockpit turn (license LIVE) · **WorkOS** chip (optional)

---

## Devpost paste — sponsor tools (featured CR)

- **CodeRabbit** — public `biobitworks/braintona`; `coderabbitai[bot]` on PRs; Discord Path B Best Use lane; observe hop sealed into token trace (does not rewrite content leaf)  
- **Daytona** — independent sandbox recompute + fail-closed tamper  
- **Braintrust** — gold-task eval spans (`braintona-hacksprint`)  
- **Fireworks AI** — claim extraction (`glm-5p1`)  
- **ElevenLabs** — two-avatar call + voice-origin FCO leaves  
- **CopilotKit** — operator FCO cockpit (org/license path)  
- **WorkOS** — optional AuthKit identity (not vault SoT)  
